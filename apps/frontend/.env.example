# Base URL the SPA calls. In dev, leave as /api (Vite proxies to the backend).
VITE_API_URL=/api
# Where the Vite dev-server proxy forwards /api during `npm run dev`.
VITE_API_PROXY=http://localhost:3000
